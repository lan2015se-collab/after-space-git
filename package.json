{
  "name": "after-space",
  "version": "1.4.0",
  "description": "AFTER SPACE installer build source",
  "main": "main.js",
  "author": "Codex",
  "license": "MIT",
  "scripts": {
    "build": "electron-builder --win nsis",
    "dist": "electron-builder --win nsis",
    "start": "electron .",
    "prepare:mobile": "powershell -ExecutionPolicy Bypass -File scripts/prepare-mobile.ps1",
    "android:sync": "npm run prepare:mobile && npx cap sync android",
    "android:apk": "npm run android:sync && powershell -ExecutionPolicy Bypass -File scripts/build-android.ps1",
    "android:release": "npm run android:sync && powershell -ExecutionPolicy Bypass -File scripts/build-android-release.ps1"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "electron": "^37.10.3",
    "electron-builder": "^26.8.1",
    "png-to-ico": "^3.0.1"
  },
  "build": {
    "appId": "com.afterspace.game",
    "productName": "AFTER SPACE",
    "asar": false,
    "directories": {
      "output": "dist"
    },
    "files": [
      "*.js",
      "*.html",
      "*.css",
      "assets/**/*",
      "build/**/*",
      "vendor/**/*",
      "node_modules/three/**/*"
    ],
    "icon": "build/app-icon.ico",
    "win": {
      "icon": "build/app-icon.ico",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "shortcutName": "AFTER SPACE",
      "installerIcon": "build/app-icon.ico",
      "uninstallerIcon": "build/app-icon.ico",
      "installerHeaderIcon": "build/app-icon.ico",
      "installerSidebar": "build/installer-sidebar.bmp",
      "uninstallerSidebar": "build/installer-sidebar.bmp"
    }
  }
}
